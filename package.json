{
  "name": "democypress",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "testd": "echo \"Error: no test specified\" && exit 1",
    "cy:open": "cypress open --config watchForFileChanges=true DEBUG=cypress:*",
    "test": "cypress run --headless --browser chrome"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^9.5.2",
    "cypress-iframe": "^1.0.1"
  }
}
